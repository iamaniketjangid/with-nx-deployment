{
  "name": "shared",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "shared/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo '🚀 Shared lib build started' && sleep 4 && echo '✅ Shared lib build completed'"
      }
    }
  },
  "tags": []
}
