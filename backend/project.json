{
  "name": "backend",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "backend/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo '🚀 Backend build started' && sleep 6 && echo '✅ Backend build completed'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo '🧪 Backend tests running' && sleep 3 && echo '✅ Backend tests passed'"
      }
    }
  },
  "tags": []
}
